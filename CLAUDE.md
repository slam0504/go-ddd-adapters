# Claude Notes for go-ddd-adapters

Read `AGENTS.md` first. It points to the shared workspace protocol and the
repo-local `.agent/` memory files.

For cross-repo work with `go-ddd-core`, also read:

- `<workspace-root>/.agent-memory/go-ddd.md` (`<workspace-root>` is defined in `AGENTS.md`)
